# Create PDF from React Component

This project is a test to create and download a PDF document from a copy of a React Component.

## Available Scripts

In the project directory, you can run:

### `yarn start`
